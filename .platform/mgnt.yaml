base:
  global:
    istioNamespace: istio
    # hub: docker.io/istio
    # tag: 1.25.2

istiod:
  global:
    istioNamespace: istio

gateway:
  service:
    annotations:
      service.beta.kubernetes.io/aws-load-balancer-type: "nlb"